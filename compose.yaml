services:
  mirror:
    build: .
    ports:
      - "8788:8788"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    volumes:
      - ./public:/app/public:ro
      - ./functions:/app/functions:ro
