{
  "name": "mirror-app",
  "version": "1.0.0",
  "private": true,
  "description": "Mirror is your queer-glam confidence closet: bold fits, fearless vibes, and main-character energy in every look.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "build": "echo 'Static app - no build step'",
    "mobile:sync": "npx cap sync",
    "mobile:android": "npx cap open android",
    "mobile:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "wrangler": "^4.0.0"
  }
}
